version: 0.1
component: build
timeoutInSeconds: 1000
shell: bash

steps:
  - type: Command
    name: "compress the repo"
    command: |
      tar cvzf /tmp/repo.tgz ./ >> /var/null
      echo $0
      ls /tmp
outputArtifacts:
  - name: artifact
    type: BINARY
    location: /tmp/repo.tgz
